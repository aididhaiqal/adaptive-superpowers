# Model profiles

Version 0.1.0 ships no model-specific behavioral overrides. Add the smallest possible profile only after repeated evidence shows that one model fails a shared contract that the other target models follow.
