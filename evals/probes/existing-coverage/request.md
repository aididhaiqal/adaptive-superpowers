Continue the profile formatter fix after our context compaction. The accepted requirement is to trim outer whitespace from a string and return `Anonymous` when it is blank, keeping the existing CommonJS export. The remaining change is in `src/profile.js`; the repository's existing tests describe the expected behavior. Finish the fix and verify it. No delivery action was requested.
