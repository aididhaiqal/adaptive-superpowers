# Claude Fable

Fable uses the Claude Code adapter; it is a model selection, not a separate host. Evaluations invoke Claude Code with `--model claude-fable-5`. No Fable-specific behavioral profile is shipped initially.
